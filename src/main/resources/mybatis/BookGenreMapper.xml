<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.app.mapper.BookGenreMapper">

	<!-- 書籍ジャンル一覧 -->
	<select id="selectAll" resultType="BookGenre">
		SELECT *
		FROM book_genres
		ORDER BY id ASC
	</select>

</mapper>